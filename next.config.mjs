/** @type {import('next').NextConfig} */
const nextConfig = {
  //   webpack: (config) => {
  //     config.resolve.extensions.push(".cjs");
  //     return config;
  //   },
};

export default nextConfig;
